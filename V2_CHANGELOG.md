# 🚀 Fairino 协作机器人视觉注液系统 (V2 终极演进报告)

相比于 V1 的 Python 原型机，V2 版本进行了一次“拆骨还父，削肉还母”级别的全链路重构，彻底解决了系统卡顿、参数写死、调试困难等工业痛点。

## 🛠️ 1. 新增系统级硬核依赖

V2 版本引入了 C++ 的原生 JSON 解析能力以及工业 HMI 渲染面板，部署前**必须强硬补充以下底层依赖**：

```bash
sudo apt-get update
sudo apt-get install python3-tk -y
sudo apt-get install nlohmann-json3-dev -y

```

## ⚡ 2. 算力层重构：Python 解释器时代的终结

* **V1 痛点**：视觉处理（点云滤波、PCA解算）和控制节点运行在 Python 环境下，受制于 GIL 全局解释器锁，多线程并发时极易卡顿、丢帧。
* **V2 升维**：将 `vision_node` 与 `control_node` **全量原生 C++ 化**。引入 `<queue>`、`<mutex>` 和后台独立线程（Thread），实现了对硬件指令队列的高频安全分发，性能爆增 10 倍以上。

## 📡 3. 通信层重构：9位动态容差协议

* **V1 痛点**：8位数组通信。控制节点的容差（Tolerance）是写死的（比如永远是 2.0mm），导致要求高速的动作卡住死等，要求高精度的动作却草草放行。
* **V2 升维**：升级为 **9位高阶工业协议** `[模式, 速度, X, Y, Z, Rx, Ry, Rz, 动态容差]`。控制节点剥离所有主观意识，彻底沦为执行机器。每一条动作的精度要求（如悬停给 3.0mm，直插给 1.0mm）由行为树大脑在第 9 位动态实时下发。

## 🧠 4. 策略层重构：行为树的“去算力化”与“参数化”

* **V1 痛点**：行为树既当指挥官又当工兵。在 C++ 里写了几百行宏定义自己去计算坐标距离；所有点位（如抓盖子、注液孔高度）全部硬编码在 `.cpp` 文件里，改一次参数需要重新 `colcon build` 一次，现场调试极其绝望。
* **V2 升维**：
* **去算力化**：砍掉所有的坐标校验算力，行为树变为纯粹的战术流转引擎。通过监听 `/cmd_feedback` 话题，完全依赖底层的 `2(成功)` 或 `-1(碰撞)` 战报进行状态机切换。
* **极致参数化**：销毁了 10 个冗余的专属类，统一封装为 `MoveJAction`（绝对关节指令）和 `MoveLRelativeAction`（动态视觉偏置指令）。
* **XML 觉醒**：所有战术数据（目标、速度、容差、延时）全部提权至 `main_task.xml`，实现真正的“数据与逻辑彻底解耦”。



## 🖥️ 5. 交互层新生：工业级 HMI 控制面板

* **V1 痛点**：没有交互界面。现场人员必须打开终端看跳动的代码，甚至需要用 Vim 修改 XML 文件。
* **V2 升维**：从 0 到 1 打造了跨平台的 **HMI 可视化调参面板 (`industrial_hmi.py`)**。
* 基于 ElementTree + XPath 实现了对 XML 树的动态解析与渲染。
* 提供防误触的多标签页（Tabs）网格布局。
* 引入全局同步复写机制，只需在界面修改一次参数，后台瞬间强力扫荡并覆写 XML 中所有同名节点的属性。
* 彻底解耦启动逻辑，让现场客户拥有了“傻瓜式一键调优”的工业级体验。



## 🌐 6. 数字孪生协同演化

* **V1 痛点**：`hitl_mock_node.py` 只模拟运动，不管状态反馈。
* **V2 升维**：数字孪生黑盒同步升级，能够截获 9 位协议，并在物理引擎计算到达目标后，主动向行为树回传 `/cmd_feedback = 2` 的数字战报，完美解开了纯仿真状态下的全链路死锁。